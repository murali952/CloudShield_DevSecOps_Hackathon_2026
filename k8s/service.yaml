apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: database-service
spec:
  type: LoadBalancer
  selector:
    app: database-service
  ports:
    - port: 80
      targetPort: 8080
